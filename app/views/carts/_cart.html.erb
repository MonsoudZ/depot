<div id="<%= dom_id cart %>">
  <h2 class="w-full sm:w-auto my-5 space-y-5">Your Pragmatic Cart</h2>

  <ul class="font-bold text-lg mb-3">
    <%cart.line_items.each do |line| %>
      <li><%=line.product.title %></li>
    <% end %>
  </ul>
</div>
